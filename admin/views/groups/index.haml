.col-md-6
  %h3
    Länder
.col-md-1.col-md-offset-1
  = link_to 'Neu', url(:groups, :new), :class => 'btn btn-success pull-right'

%p
%p

.col-md-12.top-buffer
  - @groups.each do |group|
    .col-md-6
      = link_to group.name, url(:posts, :index, :group_id => group.id)
    .col-md-1
      = link_to 'edit', url(:groups, :edit, id: group.id), class: 'btn btn-default'
    .col-md-1
      = form_for :group, url(:groups, :destroy, id: group.id), method: :delete do |f|
        = f.submit 'delete', class: 'btn btn-danger'
