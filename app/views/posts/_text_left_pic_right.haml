.row.full-height
  .col-md-6
    %h3= post.title
    %p.text= post.content
  .full-height.col-md-6
    - if (picture = post.picture)
      = image_tag picture.url(:huge), :class => 'img-responsive center-block'
