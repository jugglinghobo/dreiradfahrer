.row.title-row
  .title.col-md-12
    %h1.text-center= post.title
- if (picture = post.picture)
  .row.below-title-row
    .full-height.col-md-12
      = lazy_image_tag picture.url(:huge), :class => 'img-responsive center-block'
